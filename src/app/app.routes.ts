import { Routes } from '@angular/router';
import {LoginComponent} from './fetures/login/login/login.component';

export const routes: Routes = [{
  path:"",
  component: LoginComponent,
  title:"Login"
}];
